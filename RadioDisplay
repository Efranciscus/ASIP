import React from 'react';

export default function RadioDisplay({ powered, display, sub, badge, xmit }) {
  return (
    <div className="rounded-md p-1.5 bg-[#15190f] shadow-[inset_0_2px_6px_rgba(0,0,0,0.85)]">
      <div
        className="relative rounded-sm h-[96px] flex items-center px-5 border border-black/50 transition-colors duration-300"
        style={{ background: powered ? '#8e947c' : '#2a2f24' }}
      >
        <div className="flex flex-col flex-1">
          <div
            className="font-mono text-3xl tracking-[0.16em] text-[#2d3128] leading-none transition-opacity duration-200"
            style={{ opacity: powered ? 1 : 0 }}
          >
            {display || '\u00A0'}
          </div>
          <div className="font-mono text-[11px] tracking-[0.18em] text-[#2d3128]/70 mt-2 h-4">
            {powered ? sub : ''}
          </div>
        </div>
        {badge && (
          <div
            className="font-mono text-[11px] tracking-widest px-2 py-1 rounded border border-[#2d3128]/60 text-[#2d3128]"
            style={{ opacity: powered ? 1 : 0 }}
          >
            {badge}
          </div>
        )}
      </div>
    </div>
  );
}
